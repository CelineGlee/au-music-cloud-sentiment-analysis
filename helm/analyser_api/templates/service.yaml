apiVersion: v1
kind: Service
metadata:
  name: {{ include "analyser-api.fullname" . }}
  labels:
    {{- include "analyser-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "analyser-api.selectorLabels" . | nindent 4 }}
